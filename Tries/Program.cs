namespace Tries
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Trie trie = new Trie();
            trie.Insert("babe");
            trie.Insert("baby");

            //try pattern (functions)
        }
    }
}